# PRDs

This folder holds scoped PRDs for larger initiatives/epics. The current top-level PRD lives at repository root: `PRD.md`.

Recommended use:
- Use root `PRD.md` for the product-wide requirements.
- Use `docs/PRD/*` for feature PRDs once scope stabilizes (e.g., Supplier Portal, Exports, Compliance).

